\title{parallel (or the lack thereof) insertion sort}
\taxon{Theorem}
\import{base-macros}

\p{
    There is no parallelism benefit to insertion sort, so its span is the same as its work: For all #{l : \list{A}}, we have that \isBounded{\isort{l}}{(|l|^2, |l|^2)}.
}